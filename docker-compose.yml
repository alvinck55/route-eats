services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: routeeats
      POSTGRES_USER: routeeats
      POSTGRES_PASSWORD: routeeats
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
